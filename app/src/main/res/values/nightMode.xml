<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="NightMode" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColor">@color/nightText</item>
        <item name="android:background">@color/nightBackground</item>
        <item name="android:colorButtonNormal">@color/nightButton</item>
    </style>

</resources>

